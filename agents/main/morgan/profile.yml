# morgan — leader profile (model / effort / cadence). Persona lives in system_prompt.md.
model: deepseek-v4-pro        # strong reasoning — integration + layered review (§7.1)
effort: ultra
# Schedule is defined in evva-swarm.yml (leader block). morgan is primarily event-driven
# (engine webhooks the §6.3 triggers to the leader); the cron there is a safety-net patrol.
